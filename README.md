# Складський облік — PHP/MariaDB MVC
Веб-додаток для кількісного обліку руху матеріалів на складах з автоматичним розрахунком списання за витратою ресурсів.

--------------------------------------------------------------------------------------------------------------------------
## Журнал змін (останні зверху)

**Журнал змін (останні зверху)**

--------------------------------------------------------------------------------------------------------------------------

### Локальна авторизація (червень 2026)

- Додано можливість роботи без Nextcloud через єдину точку входу index.php
- Сторінка логіну login.php з перевіркою пароля (паролі хешуються через password_hash)
- Паролі зберігаються в таблиці user_roles (поля password_hash, is_local, email)
- Користувач може змінити пароль через дашборд (модальне вікно з AJAX, метод DashboardController::changePassword)
- Кнопка "Вихід" на дашборді поруч з останнім входом
- Маршрут logout в index.php

### Кнопка синхронізації контролерів (червень 2026)

- Додано кнопку "Синхронізувати" в адмінку adminMenu
- Метод MenuModel::syncFromFiles() сканує папку controllers/ і додає нові контролери в menu_items
- Нові контролери додаються без групи (parent_id = NULL) з sort_order = 0
- Експорт/імпорт/хелпери автоматично виключаються з синхронізації

### Фільтрація за правами доступу (червень 2026)

- Додано підтримку обмежень по складах (allowed_warehouses) з логікою:
  - Один склад – всі рухи з ним (прихід, списання, переміщення)
  - Декілька складів – тільки прихід/списання/переміщення між цими складами
- Фільтрація додана в MovementsController, ResourcesController, ReportsController, WarehousesController, MaterialsController
- Метод PermissionManager::getAllowedWarehouses() для отримання списку дозволених складів

### Автоматичне створення прав для нового користувача (червень 2026)

- При першому вході користувач отримує роль viewer з правами view на dashboard, movements, resources, reportWarehouse, reportMaterial, reportResource
- Інші пункти меню – none (не видно)

### Виправлення (червень 2026)

- Виправлено конфлікт трейтів – метод isAjax залишено тільки в MovementValidationTrait
- Виправлено редирект після збереження періоду – тепер повернення на ту ж сторінку з оновленим діапазоном дат
- Виправлено регістрозалежність при синхронізації контролерів

--------------------------------------------------------------------------------------------------------------------------

### Система прав доступу та динамічного меню (оновлення від 04.06.2026)

Що зроблено:

Таблиці БД
menu_items - зберігає пункти меню (групи, назви, порядок, URL)
user_roles - ролі користувачів (manager, viewer, fuel) та обмеження по складах/матеріалах/типах ресурсів
user_menu_permissions - права доступу до пунктів меню (none/view/edit)

Динамічне меню

Меню будується з таблиці menu_items замість хардкоду в layout.php

Групи меню: Документи, Звіти, Довідники, Система

Пункти меню показуються тільки якщо користувач має права (view/edit)

Розподіл прав

Адміністратори (група admin в Nextcloud) мають повний доступ
Для звичайних користувачів права налаштовуються через адмінку
Рівень view дозволяє тільки перегляд (GET запити)
Рівень edit дозволяє всі операції (GET+POST)

Нові контролери

Окремі контролери для кожного пункту меню:
ReportWarehouseController, ReportMaterialController, ReportResourceController
ResourceTypesController, ResourceRatesController
AdminUsersController, AdminMenuController, AdminPermissionsController, AdminBackupController, AdminRestoreController
Всі контролери використовують трейт AuthorizeTrait

Адмінка

adminUsers - керування користувачами (ролі, обмеження)
adminMenu - редагування назв пунктів меню, видимість, потреба в діапазоні дат
adminPermissions - налаштування прав доступу до пунктів меню
adminBackup / adminRestore - бекап та відновлення БД

Фільтрація в контролерах

Додано методи filterWarehouses(), filterMaterials(), filterResourceTypes() в трейті

Використовуються в ReportsController, MovementsController, ResourcesController

Структура URL (camelCase):

/adminUsers - список користувачів
/adminUsers/edit/1 - редагування користувача
/adminMenu - управління меню
/adminPermissions - права доступу
/resourceTypes - типи ресурсів
/resourceRates - норми списання
/reportWarehouse - звіт по складу
/reportMaterial - звіт по матеріалу
/reportResource - звіт по ресурсу

### Мобільна адаптація та покращення UI (травень 2026)
- **Додано кнопку-гамбургер** для відкриття сайдбару на мобільних пристроях
- **Autocomplete на мобільних** – замінено на нативний select для уникнення проблем з клавіатурою
- **Головний діапазон дат** – кнопки пресетів (Поточний місяць, Минулий місяць, Сьогодні, Поточний рік) тепер у сітці 2×2 однакової ширини
- **Мета-інформація в модалках** – автор та дата останньої зміни/створення відображаються дрібним шрифтом в одному рядку з кнопками
- **Виправлено групування в звітах** – рекурсивне розгортання/згортання через класи та data-атрибути

### Рефакторинг CSS (травень 2026)
- **Єдиний файл `style.css` (25KB) розбито на модульну структуру:**
  - `base/variables.css` – змінні та скидання стилів
  - `base/layout.css` – основна структура (app, sidebar, main)
  - `components/buttons.css` – всі кнопки
  - `components/cards.css` – картки та заголовки сторінок
  - `components/forms.css` – форми та фільтри
  - `components/autocomplete.css` – випадаючі списки
  - `components/modals.css` – модальні вікна та date-panel
  - `components/tables.css` – таблиці та сортування
  - `components/badges.css` – бейджі та індикатори
  - `components/filters.css` – спеціальні фільтри (чекісти)
  - `pages/reports.css` – стилі для звітів
  - `pages/resources.css` – стилі для ресурсів
  - `utils/animations.css` – анімації
  - `utils/print.css` – стилі для друку
  - `utils/responsive.css` – медіа-запити
- **Головний файл `main.css`** – імпортує всі модулі
- **Всі інлайн-стилі з view винесено в CSS-файли**
- **Єдиний стиль для всіх звітів** – однакове оформлення таблиць, кнопок та фільтрів

### Звіт по використанню ресурсів (травень 2026)
- **Новий звіт** – деталізація витрати ресурсів по матеріалах та складах
- **Групування** трирівневе: Матеріал → Склад → Дні (з можливістю розгортання/згортання)
- **Колонки звіту**: 
  - Лічильник (показник одометра/таймера)
  - Δ ресурсу (витрата ресурсу за період)
  - Норма списання
  - Поправка (у %)
  - Вхідне сальдо, Прихід, Витрата, Вихідне сальдо
- **Фільтри**: тип ресурсу (мотогодини, пробіг тощо), період
- **Множинний вибір складів** – всі склади, що мають норми для вибраного типу ресурсу
- **Експорт в Excel** – два листи:
  - `Деталізація` – ієрархічна структура як на екрані
  - `Для зведеної` – плоска таблиця для зведених таблиць Excel
- **Друк** – оптимізований для паперового формату
- **Кнопки** "Все розгорнути" / "Все згорнути" для зручної навігації
- **Додаткове відображення годин** – для типів ресурсів з форматом `dec2` та одиницею `год` можна ввімкнути показ витрати також у годинах:хвилинах (галочка "Додатково в годинах")

### Уніфікація форм та звітів (травень 2026)
- **Всі довідники** (склади, матеріали) приведено до однакового стилю:
  - Однакова структура таблиць
  - Однакові кнопки дій (редагувати, видалити)
  - Однакове відображення порожнього стану (empty-state)
- **Всі звіти** (по складу, по матеріалу, по ресурсу) мають єдиний стиль:
  - Однакові фільтри та кнопки
  - Однакове групування зі стрілочками
  - Однакові підсумкові рядки та картка-футер
- **Всі модальні вікна** мають єдине оформлення:
  - Однакові заголовки, кнопки, відступи
  - Мета-інформація (автор, дата) внизу в одному рядку з кнопками
- **Сторінка "Типи ресурсів"** доповнена колонкою "В годинах" та галочкою "Додатково в годинах"

### Додано мета-інформацію в таблиці
- Таблиця `resource_rates` доповнена полями: `author`, `created_at`, `updated_at`
- Таблиця `resource_types` доповнена полем `show_hours` (галочка "Додатково в годинах")

### Виправлення логіки розрахунку балансу
- Виправлено врахування рознесених по днях списань (`spread_by_day`)
- Баланс розраховується накопичувально на основі всіх рухів (надходження + списання)
- Ручні списання виділяються помаранчевим кольором з позначкою ⚠️

### Рефакторинг коду — розбиття великих файлів (травень 2026)
- **JavaScript** (`app.js` 25KB → розбито на модулі):
  - `core/sidebar.js`, `core/date-panel.js`, `core/modal.js`, `core/ajax.js`, `core/utils.js`
  - `components/autocomplete.js`, `components/type-indicator.js`
  - `modals/warehouse.js`, `modals/material.js`, `modals/movement.js`, `modals/delete.js`, `modals/import.js`
  - `main.js` — точка входу

- **Controllers**:
  - `MovementsController.php` (28KB) → розбито на:
    - `MovementsController.php` (8KB) — CRUD, фільтри, сортування
    - `MovementsImportController.php` (6KB) — імпорт з Excel
    - `MovementsExportController.php` (2KB) — експорт
    - `traits/MovementRedirectTrait.php`, `traits/MovementValidationTrait.php`
  
  - `ResourcesController.php` (25KB) → розбито на:
    - `ResourcesController.php` (7KB) — журнал показників (CRUD)
    - `ResourceTypesController.php` (3KB) — типи ресурсів
    - `ResourceRatesController.php` (5KB) — норми списання
    - `ResourceExportController.php` (3KB) — експорт
    - `traits/ResourceValidationTrait.php`, `traits/ResourceContextTrait.php`

  - `SimpleController.php` (12KB) → розбито на:
    - `SimpleController.php` (4KB) — основний контролер
    - `SimpleDataHelper.php` (4KB) — отримання даних по складу
    - `traits/SimpleConfigTrait.php`, `traits/SimpleResponseTrait.php`

  - **Нові контролери для звіту по ресурсах**:
    - `ResourceReportController.php` — основний контролер звіту
    - `ResourceUsageExportController.php` — експорт в Excel

- **Models**:
  - `ResourceModel.php` (35KB) → розбито на:
    - `ResourceModel.php` (3KB) — фасад
    - `ResourceTypesModel.php` (2KB) — CRUD типів ресурсів
    - `ResourceRatesModel.php` (3KB) — норми списання
    - `ResourceLogsModel.php` (5KB) — журнал показників
    - `ResourceReportModel.php` (5KB) — звіти
    - `traits/ResourceFormatTrait.php`, `traits/ResourceChainTrait.php`

  - **Нові моделі для звіту по ресурсах**:
    - `ResourceUsageReportModel.php` — логіка збору даних для звіту

- **Helpers**:
  - `XlsxParserHelper.php` (3KB) — парсинг Excel
  - `XlsxGeneratorHelper.php` (4KB) — генерація Excel

- **Оновлений роутер** (`index.php`):
  - Додано автозавантаження папки `helpers`
  - Додано підміну контролерів для `Movements`, `Resources`, `Reports`
